version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: horror-quests-landing
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - horror-network

networks:
  horror-network:
    driver: bridge

